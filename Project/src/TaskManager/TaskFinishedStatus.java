package TaskManager;

/**
 * The TaskFinishedStatus class describes an enumerator class
 * that lists the statuses when a task has finished. There
 * are three possible statuses for a project, namely early, on time or with a delay.
 * 
 * @author Groep 8
 */
public enum TaskFinishedStatus {
	EARLY,ON_TIME,WITH_A_DELAY
}
